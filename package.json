{
  "name": "leban-wine",
  "version": "0.1.0",
  "description": "The site for LEBAN's wine",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "start": "http-server ./_site/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Neil Leeson Syiemlieh",
  "license": "ISC",
  "dependencies": {
    "http-server": "^13.0.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1"
  }
}
